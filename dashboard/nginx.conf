server {
    listen 80;

    server_tokens off;

    more_clear_headers Server;

    root /usr/local/openresty/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}